In order to run PHPUnit tests you need to use the custom list of tests for .
Some standard tests will fail in an  installation. These have been replaced
with special  versions. 

vendor/bin/phpunit -c iomad_phpunit.xml
